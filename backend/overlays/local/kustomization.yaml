apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  env: local

resources:
  - ../../base

configMapGenerator:
  - name: auth-microservice-configmap
    envs:
      - file/backend.env
    behavior: merge
  - name: postgres-configmap
    envs:
      - file/backend.env
    behavior: merge
